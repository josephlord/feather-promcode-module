import XCTest
@testable import feather_promocode_module

final class feather_promocode_moduleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(feather_promocode_module().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
